# === BASE DE DONNÉES ===

# Mode Local (Développement) - SQLite
# DATABASE_URL=./data/crm.db
# ou
DATABASE_URL=file:./data/crm.db

# Mode Production - Turso (LibSQL)
# Créer une base sur turso.tech puis :
# DATABASE_URL=libsql://YOUR_DB.turso.io?authToken=YOUR_TOKEN
# Voir TURSO_SETUP.md pour les instructions

# Token Turso (optionnel si dans DATABASE_URL)
# TURSO_CONNECTION_TOKEN=eyJ...

# === AUTHENTIFICATION ===

# Secret utilisé pour signer les cookies de session.
# En production, remplace-le par une chaîne aléatoire d'au moins 32 caractères.
# Générer avec : node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
AUTH_SECRET=change-moi-avec-une-chaine-aleatoire-de-32-caracteres-minimum

# Mot de passe attribué aux 3 comptes lors du seed initial
SEED_PASSWORD=crm2026
